import { Router } from "express";

const router = Router();

// TODO (Etapa 4): verificação do webhook exigida pelo Meta (GET) e
// recebimento das mensagens do WhatsApp (POST), usando WHATSAPP_VERIFY_TOKEN.

export default router;
